# 城市外语标识纠错平台

本项目面向市民、志愿者、语言专家和标识设置单位，保存公共标识纠错线索与整改版本。仓库提供运行状态接口和领域字段样例，图片样例只描述必要元数据，不包含真实个人信息。

运行服务：

```bash
npm start
```

运行检查：

```bash
npm test
```

`fixtures/sign-report.json` 展示标识、语种和位置授权的字段约定。面向公众的页面只能呈现获准公开的信息，内部联系方式不得出现在公开响应中。
